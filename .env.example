BACKEND_URL=http://localhost:8000

# Optional: HTTP Basic Auth gate (set BOTH to enable, leave unset to disable).
# Used for pre-launch private previews on Vercel. Don't set in local dev.
# BASIC_AUTH_USER=
# BASIC_AUTH_PASSWORD=
